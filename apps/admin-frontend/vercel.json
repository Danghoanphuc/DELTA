{
  "buildCommand": "cd ../.. && pnpm --filter @printz/types build && pnpm --filter @printz/utils build && pnpm --filter @printz/ui build && pnpm --filter admin-frontend build",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://printz-admin-backend-staging.onrender.com/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
