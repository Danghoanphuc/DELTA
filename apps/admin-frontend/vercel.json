{
  "buildCommand": "pnpm --filter admin-frontend build",
  "outputDirectory": "apps/admin-frontend/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

