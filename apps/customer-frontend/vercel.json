{
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "chmod +x ./prepare-vercel.sh && ./prepare-vercel.sh",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
