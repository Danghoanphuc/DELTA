{
  "buildCommand": "pnpm --filter @printz/types build && pnpm --filter customer-frontend build",
  "outputDirectory": "dist",
  "installCommand": "corepack enable pnpm && corepack prepare pnpm@9.0.0 --activate && pnpm install --filter customer-frontend...",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
