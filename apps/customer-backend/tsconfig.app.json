{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "noEmit": false, // ✅ FIX: Override noEmit để cho phép emit files khi build
    "types": ["node", "bull", "connect-mongo"]
  }
}
